version: "3.9"

services:
  python-service:
    build: .
    container_name: tp-inf462-python-service
    ports:
      - "8001:8001"
    volumes:
      - ./app/storage:/app/app/storage
    env_file:
      - .env